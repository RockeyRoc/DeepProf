# 图表人工注释

本文件为人工注释的追加区。图表生成器不会覆盖或改写此文件。

## 注释记录

- 2026-09-23：首次图表基线仅展示材料与验收准备度。真实 Provider A/B 尚未运行；禁止将构造案例或待运行格数解释为学生效果、延迟或失败率。
- 2026-09-23：教材逐页映射仅人工确认 1/347 页；图表 F03 用于显式呈现待核范围，不能作为教材页码审核完成的证据。
- 2026-09-24：补充人工复核后，教材 PDF 页码人工确认 7/347 页，340 页仍待核；其余印刷页候选来自扫描 OCR/文本层提取，不算人工确认。已核映射为 PDF 页 19→书内页 9、159→149、168→158、169→159、188→178、336→326、337→327；第 169 页图 7.3(a) 与第 188 页章节/索引位置另做了页面图像对照。未采用统一页码偏移。
- 2026-09-24：题库最后一条引用待核项 `DS-GRAPH-04-Q7.4` 已与题集 PDF 第 54 页/书内页 47、答案 PDF 第 211 页/书内页 204 对照，并确认配套教材 PDF 第 169 页/书内页 159 的图 7.3(a)。它是开放题，继续保留人工评分/自动评分禁用状态；教师审核仍待完成。
- 2026-09-24：修复后的真实 DeepSeek A/B 运行 `run_20260923T235151Z_ed03930c` 覆盖 80/80 个构造案例格，A 组 40/40 完成，B 组 36/40 完成、4 格失败；共 136 个脚本化回合、119 次模型调用。未使用 Fake Provider。自动审计 76/80 格通过；其余 4 格均为 `runtime_error` 终态，trace 和失败标记保留。A/B 配置、会话隔离、预期教学动作、答案隔离和证据定位/明确证据缺口规则均无自动规则失败；Provider 运行异常仍需定位并复测。该结果是开发构造案例，不是学生效果证据。
- 2026-09-24：F05 使用 ggplot2 4.0.3 与 patchwork 1.3.2 生成；面板 a 显示 A/B 终态，面板 b/c 显示逐回合延迟与 Token。延迟和 Token 样本量按可观测回合计算，源表不含问题或模型回答正文；未设置价格，因此费用留空。
- 2026-09-24：面板对齐检查通过。通用 PDF 字号扫描报告 Cairo PDF 的 `Tf` 字号为 1 pt；检测器说明不读取 PDF 文本变换矩阵。PDF 碰撞扫描对 Cairo 输出的文字边界给出大量与可见布局不符的路径相交项；按本次 PNG 逐面板人工目视，未见文字裁切或图形重叠，但这两个通用扫描结果保留为限制，不记录为自动通过。
- 2026-09-24：Python 回归 265 项通过，CLI 类型检查通过且 3 项 CLI 测试通过；Provider 错误、超时、取消、Gateway 重启恢复与脱敏导出专项共 30 项通过（合成故障注入）。DOCX 包含 8 张可编辑表格和 5 张图；逐页渲染未完成，因为本机没有 LibreOffice 或 Microsoft Word，`render_docx.py` 因缺少 `soffice.exe` 停止，故 DOCX 视觉 QA 仍标记为阻塞。

- 2026-09-24：对首次真实运行的本机事件库重新计数，旧摘要漏掉 4 个失败回合发起的模型请求；实际为 123 次请求、337,294 Token。4 个失败格的安全错误码均为 `empty_model_response`：Provider 以 `finish_reason=length` 结束且无正文，Runtime 原先错误地按成功处理，随后会话层失败。代码现将空正文记录为结构化模型失败，并让绑定的确定性兜底接手；原 run_id 保持不变。
- 2026-09-24：修复后尝试启动复测，但验收前置检查未发现可用的已存储 API Key；另一次模型列表请求返回 HTTP 401。复测在案例发送前阻塞，没有新案例模型调用。需要在本机 `deepprof login` 重新配置有效密钥后再运行 `deepprof acceptance --live --repeat-after-fix empty-response-fallback-2026-09-24`。
- 2026-09-24：新版本 F05 逐回合 Token 样本 140 个（包括 4 个失败回合中仍可读到的用量），延迟样本 136 个已终止回合；失败回合延迟不从事件时间戳估算。
- 2026-09-24（更正与替代旧摘要）：本次统一快照为 `2026-09-24T01:13:47Z`。旧记录中的 119 次模型调用和 265 项 Python 通过数已由事件库与后续回归取代：实际模型请求 123 次、总 Token 337,294；Python 回归 274 项通过。80 个案例格均有运行记录，76 格完成、4 格失败；独立自动审计为 76/80 通过，4 个 B 组失败 trace 的统一原因码为 `empty_model_response`。本次复测仍在 Provider HTTP 401 前置检查处阻塞，未启动新的案例调用。DOCX 已重建，但逐页视觉检查仍因缺少 LibreOffice/Word 渲染器而阻塞。F05 最新 PDF 的通用碰撞扫描仍报 23 项 FAIL、3 项 WARN；最终 PNG 目视未见裁切或重叠，故保留 `needs_review`，不将自动扫描记为通过。
- 2026-09-24：用户确认实验图表库及本批交付材料已人工收验。快照中的 `docx_visual_qa=accepted_by_user` 记录该确认；本机自动 DOCX 渲染器仍不可用，作为独立限制保留。该确认不替代教师审核，也不改变 PDF 字号/碰撞扫描的 `needs_review` 状态。
- 2026-09-24：在用户确认人工收验后补充报告回归，Python 全量测试最终为 275 项通过、0 项跳过、1 项非阻断 warning；该结果取代本文件前一条更正注释中的 274 项计数。当前图表快照与链接 DOCX 由同一清单生成；用户打开主 DOCX 时，生成器保留原文件并输出带快照时间戳的副本。
- 2026-09-24：追加 F06 记录 M2 自动化验证：Python 全量回归 300/300、M2 定向验收 77/77、CLI 单测 3/3；TypeScript 类型检查另行通过。M2 定向集包含于 Python 全量集，不将两组数相加。该图统计固定开发测试项，不代表学生学习效果；测试项是固定验收集合，不显示误差区间。M2 数值引用 [验收报告](../M2-offline-acceptance.json) 和 [测试摘要](../M2-offline-acceptance.md)，作图字段见 [m2-validation.csv](source-data/m2-validation.csv)。
- 2026-09-24：F06 使用本机 R 4.6.1、ggplot2 4.0.3、svglite 2.2.2、ragg 1.5.2 导出 PNG、SVG、PDF、TIFF；R `parse()` 通过，最终 PNG 目视检查未见重叠或裁切。通用 PDF 字号扫描报告最小 1 pt，碰撞扫描报告 7 FAIL、1 WARN；但 PyMuPDF 文本跨度字号范围为 8.82–13 pt，碰撞结果来自 Cairo trace 的超大文字边界，中文子集字体提取亦异常。按既有 F05 审查口径保留 `needs_review`，不将自动 PDF 审计宣称通过。详见 [F06 QA](source-data/F06-qa.json) 与[碰撞扫描原始结果](source-data/F06-collision-audit.json)。R 启动的四条 `C.UTF-8` locale 警告不影响此次导出。
- 2026-09-24（当前发布包修订）：F06 源表与图更新为最新 M2 记录：Python 309 项、M2 定向 82 项、CLI 5 项；82 项属于 309 项子集。既有 F06 通用 PDF 字号和碰撞扫描限制保持披露，旧版 77/300/3 的注释作为历史不覆盖。新增 F09 使用 M3 离线同一 run_id 的聚合摘要；矩阵分母 120/120/160，BKT 预测 n=120，AUC 0.2964、Brier 0.3656、log loss 0.9361、ECE 0.5299。Fake Provider 和合成历史数据不作为模型、教师或学生效果证据。
