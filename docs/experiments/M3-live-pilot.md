# M3 真实模型试点记录

日期：2026-09-24  
状态：**12×A/B/C 试点已运行；调用预算合规，但模型截断频繁，不能作为 M3 研究验收或组间效果结论。**

## 运行范围

运行 ID：`m3-live-20260924-071515-indexed-8e41c6d2`；早先没有本地课程索引的零调用预检 `m3-live-20260924-070550-9fd760a2` 保留为历史记录。试点使用 12 个版本化构造开发案例 × A/B/C，共 36 个独立会话和 36 个独立学习者；每格只提交一条用户回合，按自动路径处理。

真实 Provider 为 DeepSeek `deepseek-flash`。Gateway 仅绑定 `127.0.0.1`，每格最多一条生成请求；Provider 探测 0 次，重试 0 次，最多生成参数设为 512 tokens。实际请求 29 次，低于授权上限 36 次；另有 7 格由教学规则直接结束，没有触发 Provider 生成。原始回答和逐事件证据仍只保存在本机 `.deepprof` 运行目录；仓库导出仅含聚合字段，不含提示、教材片段、回答正文、会话/学习者/追踪 ID 或凭据。

## 运行结果

| 组 | 格数 | 应用终态完成 | 应用终态失败 | Provider 请求 | 评测完整生成 | 评测截断失败 | 无生成（不适用） | 输入 tokens | 输出 tokens | 可定位引用 / 引用 | 合成 Attempt 种子 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| A | 12 | 2 | 10 | 10 | 0 | 10 | 2 | 18,266 | 5,120 | 0 / 0 | 0 |
| B | 12 | 12 | 0 | 10 | 2 | 8 | 2 | 5,633 | 4,989 | 50 / 50 | 0 |
| C | 12 | 12 | 0 | 9 | 1 | 8 | 3 | 5,481 | 4,454 | 50 / 50 | 36 |
| 合计 | 36 | 26 | 10 | **29** | **3** | **26** | **7** | **29,380** | **14,563** | **100 / 100** | **36** |

**截断判定已修正：**Provider 请求以 `finish_reason=length` 结束时，该格的评测结果为截断失败，即使应用终态记为完成。29 次请求中只有 3 次同时具备 `model.completed` 与 `finish_reason=stop`，计为完整生成；26 次长度截断计失败；另 7 格未触发生成，完整生成指标对它们不适用。A 组 10 格应用终态失败均为截断；B/C 另有 16 格应用终态完成却截断。应用终态仍单独保留，不能替代评测结果。

每次请求均配置 `max_tokens=512`、`max_retries=0`；Provider 用量记录中有 1 格报告 513 completion tokens。该值比请求上限多 1，已作为异常保留，不能据请求参数推断服务端用量严格不超过 512。没有为此重试或补跑。全部 29 个请求均有用量数据。

总用量为 29,380 输入 tokens 与 14,563 输出 tokens，其中输入缓存命中 24,320、未命中 5,060。按 DeepSeek 当前 `deepseek-flash` 官方价目，以 Provider 回报的缓存命中/未命中数计算，峰时费率估算为 **US$0.01914**，非峰时费率估算为 **US$0.00957**；DeepProf 未配置或读取账户账单，这不是实际账单金额。费率会变化，见[官方价目](https://api-docs.deepseek.com/quick_start/pricing/)。

策略动作族与开发构造标签匹配 22/26，仅指应用策略事件与开发案例允许动作的一致性；它不是教师评分、模型回答质量或学习成效指标。B/C 的 100/100 只表示引用定位字段完整，不表示教材内容支持模型陈述。A/B 未写入 BKT；C 组在真实 Attempt/BKT 存储管线写入 36 条合成历史记录，不能据此说明 BKT 预测有效。

## 图表与脱敏数据

Provider `finish_reason` 分布见 [F08 图表](figures/F08-m3-live-pilot-results.png)；逐格截断评测判定见 [CSV](source-data/m3-live-pilot-live-cells.csv) 与[汇总 JSON](source-data/m3-live-pilot-live.json)。此前只检查本地检索、未触发 Provider 的预检见 [F07](figures/F07-m3-live-pilot-preflight.png)。图表 QA 见 [F08-qa.json](source-data/F08-qa.json)，可复现图表的脚本为 [generate_m3_live_pilot_live.R](generate_m3_live_pilot_live.R)。脱敏导出由 [export_m3_live_pilot_aggregate.py](../../scripts/export_m3_live_pilot_aggregate.py) 从本机逐格记录生成；CSV 保留应用 `terminal_status`，并另列 `evaluation_status`。脚本只输出字段白名单。

冻结配置摘要：课程 `ds.c_language.v1`；题库 `ds-c-dev-v1`；案例 `ds-m3-live-pilot-v1`（源案例 `ds-dev-cases-v1`）；策略 `ds-v0.6.2-m2.1`；提示版本 `education-prompts-source-fingerprint-v1`；温度 0.3；BKT 参数哈希 `51340c493ab91337ea65b92535dc82237a6e1209ad23a985506e5090f4e8126f`；检索索引 685 chunks。完整课程/题库/提示/案例/检索/代码 SHA-256 与每格审计值保存在脱敏 JSON 和本机 manifest 中；提示正文、检索片段和 Provider 回答不写入仓库。

运行代码指纹：commit `71c5edc24c33fe94ac5ff5fb08715bdeaf655737`；工作树在运行时已有修改。状态摘要 SHA-256 `a66f04a50ec8593af0808a24966d746b0c8063fd6129d981180f8403c9b46b49`，工作树差异 SHA-256 `e2221ad01875cd985c06d09a3c7298d9c92aaa230bdfd85f4e7cef43b24e862e`。后处理图表和导出脚本在试点完成后新增，不属于上述运行时指纹。

## 结论边界

- **M2 工程验收状态：**工程测试通过；教师审核和参数校准仍待完成，见[最新 M2 验收记录](../M2-offline-acceptance.md)。
- **M3 框架状态：**本地离线框架试跑通过；逐格评测现在把长度截断明确判为失败，并保留应用终态供审计。框架研究验收仍未完成。
- **真实模型实验状态：**本次 12×A/B/C 工程试点已完成；29 次请求中 3 次完整生成、26 次截断失败，7 格未触发生成。不能据此作模型有效性或组间效果结论。
- **教师审核状态：**待完成。没有教师标签、盲评或仲裁数据。
- **学生学习效果：**未测量；没有真实学生参与。
