# ============================================================
# npm 配置（desktop/）
#
# ⚠️ 为什么要把缓存目录搬进工程内（2026-09-21）
#
# 默认情况下 npm 把缓存写在 %LOCALAPPDATA%\npm-cache，
# 而 Electron 的 postinstall 还要往 %LOCALAPPDATA%\electron 下载二进制。
# 用户的要求是「应用最多只在用户目录里创一个 .deepprof，像 codex 那样」——
# 一个前端工程往三个地方撒状态，本身就说不清"卸载干净了没有"。
#
# 放在 desktop/.cache/ 下（已加进 .gitignore），删掉 desktop/.cache
# 就等于把构建缓存清干净了。
#
# 注意：Electron 的二进制缓存读的是环境变量 ELECTRON_CACHE /
# ELECTRON_BUILDER_CACHE，不是 .npmrc 的键。装依赖前先设好：
#
#   Windows (PowerShell):
#     $env:ELECTRON_CACHE="$PWD\.cache\electron"
#     $env:ELECTRON_BUILDER_CACHE="$PWD\.cache\electron-builder"
#     npm install
# ============================================================

cache=.cache/npm

