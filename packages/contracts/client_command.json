{
  "contract_version": "1.5.0",
  "ClientCommand": {
    "command_id": "string",
    "client_id": "string",
    "surface": "cli",
    "session_id": "string | null",
    "learner_id": "string",
    "type": "string, 见 command_types",
    "payload": "object; session.new accepts session_mode=chat|study and experiment_run boolean (internal frozen evaluations only)",
    "timestamp": "string, ISO-8601 UTC"
  },
  "command_types": [
    "message.send",
    "turn.cancel",
    "session.new",
    "session.resume",
    "session.fork",
    "session.compact",
    "quiz.generate",
    "quiz.answer",
    "library.import",
    "document.convert"
  ],
  "_surfaces": { "cli": "完整会话入口（键盘优先）" },
  "_boundary": "前端不直连 Provider/Memory/Tool；不持有 API Key；统一经 Gateway 进入 Runtime。"
}
